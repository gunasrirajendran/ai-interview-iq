version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - '8000:8000'
    environment:
      - DATABASE_URL=sqlite:///./interviewiq.db
  frontend:
    build: ./frontend
    ports:
      - '5173:5173'
